import { AutoCompleteTypeFilterPipe } from './auto-complete-type-filter.pipe';

describe('AutoCompleteTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoCompleteTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
